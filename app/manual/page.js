import ManualClient from "./ManualClient";

export const metadata = {
  title: "Manual de Usuario - Mineiro Editor",
  description: "Guía completa para usar el editor visual de Mineiro",
};

export default function ManualPage() {
  return <ManualClient />;
}
